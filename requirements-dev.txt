# Dev/test-only dependencies. Kept out of the locked runtime requirements.txt.
pytest>=8.0
pytest-cov>=5.0     # coverage measurement + fail-under floor (see pyproject [tool.coverage])
ruff>=0.5
mypy>=1.10          # static type checking (make typecheck / CI); validates the code's type hints
pre-commit>=3.7     # local ruff+mypy hooks before each commit (.pre-commit-config.yaml)
pip-audit>=2.10.1      # dependency CVE scan (advisory CI step)
selenium>=4.20      # headless-browser frontend smoke test (skips when no browser is present)
pip-tools>=7.0      # `make lock` -> requirements.lock / requirements-dev.lock
